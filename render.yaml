services:
  - type: web
    name: spelling-bee-by-anthropic
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: NODE_VERSION
        value: 20.18.0
